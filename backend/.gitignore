# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environments / lockfile-managed venvs
.venv/
venv/

# Local run script — contains secrets (D-Wave token). Never commit.
run-local.sh
.env.local
