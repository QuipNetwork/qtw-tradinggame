# Pinned dependency set for the Flux Orbit deploy (QUI-875).
#
# WHY THIS FILE EXISTS: pyproject.toml declares `>=` floors with no lockfile. The
# DigitalOcean image was built once (2026-06-26) and froze its resolution, but Orbit
# rebuilds from the git ref on every node and after every relocation — so without pins
# each rebuild would resolve *latest* and drift from the artifact that has been serving
# production. Verified drift when resolving unpinned against Python 3.12: fastapi
# 0.140.0 vs 0.138.1, numpy 2.5.1 vs 2.5.0, uvicorn 0.51.0 vs 0.49.0, dwave-optimization
# 0.7.2 vs 0.7.1, websockets 16.1.1 vs 16.0.
#
# These versions are `pip freeze` from the RUNNING production container
# (qtw-backend:48f080a, captured 2026-07-26), so an Orbit build reproduces the deployed
# dependency set exactly.
#
# Orbit picks this file up ahead of pyproject.toml: install_python_dependencies() in the
# buildpack tries uv.lock, then requirements.txt, then pyproject.toml. Do NOT add a
# uv.lock unless you intend it to take precedence over these pins.
#
# Regenerate with:  docker exec qtw-backend pip freeze
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
Authlib==1.7.2
better-profanity==0.7.0
certifi==2026.6.17
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.2
cryptography==49.0.0
dimod==0.12.22
diskcache==5.6.3
dwave-cloud-client==0.14.6
dwave-graphs==1.0.0
dwave-neal==0.6.0
dwave-optimization==0.7.1
dwave-preprocessing==0.6.11
dwave-samplers==1.8.0
dwave-system==1.35.0
fastapi==0.138.1
fasteners==0.20
greenlet==3.5.3
gurobipy==13.0.2
h11==0.16.0
homebase==1.0.1
http-sf==1.3.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
importlib_metadata==9.0.0
joserfc==1.7.1
MarkupSafe==3.0.3
minorminer==0.2.22
networkx==3.6.1
numpy==2.5.0
orjson==3.11.9
packaging==26.2
plucky==0.4.3
psycopg==3.3.4
psycopg-binary==3.3.4
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
PySocks==1.7.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
scipy==1.18.0
six==1.17.0
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uvicorn==0.49.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.0
Werkzeug==3.1.8
zipp==4.1.0

# Install the backend package itself (src-layout wheel via hatchling). Without this line
# `import backend.api.app` fails, because -r only installs the pinned dependencies.
.
